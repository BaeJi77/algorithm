#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

void sort(int left,int right,int *data){
	int temp=j;
	int j=temp;
	int i=right;

	if(left<right)
}

int main(){
	vector <int> vec;
	vec.push_back(1);
	vec.push_back(10);
	vec.push_back(12);
	vec.push_back(5);
	vec.push_back(6);
	vec.push_back(2);

	int left=0;
	int right=vec.size();

	for()


	for(int i=0;i<vec.size();i++){
		cout<<vec[i]<<endl;
	}
}
