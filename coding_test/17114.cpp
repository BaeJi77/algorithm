#include <cstdio>
#include <iostream>

using namespace std;

int N, M, T;

int main() {
    freopen("input.txt","r",stdin);
    scanf("%d %d &d", &N, &M, &T);
    for(int i = 0 ; i < N ; i++) {
        for(int j = 0 ; j < M ; j++) {
            // scanf("%d", )
        }
    }
}