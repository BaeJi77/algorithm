#include <cstdio>
#include <cstring>
#include <vector>
#include <queue>
#include <algorithm>

using namespace std;
int n,m,v,x,y;
vector <vector <int> > vt;
bool visited[10004];

queue <int> qu;
bool discover[10004];



int main(){
	freopen("input.txt" , "r" , stdin);
	scanf("%d" , &n);
	scanf("%d" , %m);
	while(m--){
		scanf("%d%d" , &x, &y);
		vt[x].push_back(y);
	}

	for(int i = 0 ; i < n ; i++){
		
	}
	
}
