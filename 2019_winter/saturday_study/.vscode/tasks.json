{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    // "command": "bash",
    // "version": "2.0.0",
    "args": ["-c"],
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "isBuildCommand" : "true",
            "command": ["g++ -o ${file} ${fileDirname}/${fileBasenameNoExtension}"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "execute",
            "type": "shell",
            "command": [""]
        }
    ]
}